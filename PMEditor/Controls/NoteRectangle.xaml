<UserControl x:Class="PMEditor.NoteRectangle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid MouseLeftButtonDown="Rectangle_MouseLeftButtonDown" MouseMove="Grid_MouseMove" KeyDown="UIElement_OnKeyDown">
        <Border x:Name="HighLightBorder" BorderThickness="0">
            <Rectangle Name="Rect"/>
        </Border>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="复制" InputGestureText="Ctrl + C" Command="local:Commands.Copy"/>
                <MenuItem Header="剪切" InputGestureText="Ctrl + X" Command="local:Commands.Cut"/>
                <MenuItem Header="删除" InputGestureText="Delete" Command="local:Commands.Delete"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
